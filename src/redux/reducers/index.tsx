import Settings from "./Setting";
import Common from "./Common";
import sliderSettingsCard from "./SliderSettings";

const reducers = {
  settings: Settings,
  common: Common,
  sliderSettingsCard: sliderSettingsCard,
};

export default reducers;
